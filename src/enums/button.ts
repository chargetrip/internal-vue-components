export enum Sizes {
  sm = "sm",
  md = "md",
  lg = "lg"
}

export enum Colors {
  note = "note",
  base = "base",
  body = "body",
  "body-alt" = "body-alt",
  alt = "alt",
  accent = "accent",
  error = "error"
}
