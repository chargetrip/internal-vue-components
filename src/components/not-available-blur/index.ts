import NotAvailableBlur from "./NotAvailableBlur.vue";

// @ts-ignore
NotAvailableBlur.install = Vue => {
  Vue.component(NotAvailableBlur.name, NotAvailableBlur);
};

export default NotAvailableBlur;
